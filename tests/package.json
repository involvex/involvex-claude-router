{
  "name": "involvex-claude-router-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for Involvex's Claude Router embeddings endpoint",
  "type": "module",
  "scripts": {
    "test": "NODE_PATH=/node_modules ./node_modules/.bin/vitest run --reporter=verbose",
    "test:watch": "NODE_PATH=/node_modules ./node_modules/.bin/vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
